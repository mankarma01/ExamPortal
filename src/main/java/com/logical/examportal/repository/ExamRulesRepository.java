package com.logical.examportal.repository;

import com.logical.examportal.entity.ExamRules;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ExamRulesRepository extends JpaRepository<ExamRules, Long> {
}
